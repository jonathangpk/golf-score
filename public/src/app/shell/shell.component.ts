import { Component } from '@angular/core';

@Component({
  selector: 'app-shell',
  template: `
    <mat-toolbar color="primary">Golf Score</mat-toolbar>
    <div>Your recent Rounds</div>
  `
})
export class ShellComponent {}
